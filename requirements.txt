# Pillow and psutil come from apt (python3-pil, python3-psutil) via
# --system-site-packages in the venv — keeps installs fast and avoids
# the libjpeg/freetype/zlib source-build dance on Pi.
luma.lcd>=2.12
gpiozero>=2.0
lgpio>=0.2
